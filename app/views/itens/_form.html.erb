<%= form_with(model: iten) do |form| %>
  <% if iten.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(iten.errors.count, "error") %> prohibited this iten from being saved:</h2>

      <ul>
        <% iten.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
 <div class="col-lg-8 form-group">   
           <%= form.label :produto_id, :class => 'control-label required' %>
           <%= form.collection_select :produto_id, Produto.all, :id, :nome, {:prompt => 'Selecionar'}, :class => 'form-control',:required=>true %> 
       </div>
  <div class="col-lg-8 form-group">   
           <%= form.label :quantidade, :class => 'control-label required' %>
           <%= form.text_field :quantidade,  :class => 'form-control' %>
       </div>
   <div class="col-lg-8 form-group">   
           <%= form.label :venda_id, :class => 'control-label required' %>
           <%= form.collection_select :venda_id, Venda.all, :id, :total_pagar, {:prompt => 'Selecionar'}, :class => 'form-control',:required=>true %> 
       </div>
       <div class="col-lg-8 form-group">   
           <%= form.label :Preço_venda, :class => 'control-label required' %>
           <%= form.text_field :preco_venda,  :class => 'form-control' %>
       </div>


  <div class="col-lg-8 form-group">   
           <%= form.label :valor_total, :class => 'control-label required' %>
           <%= form.text_field :valor_total,  :class => 'form-control' %>
       </div>

       
       

  <div class="row">
          <div class="col-lg-7 form-group">  
            <%= link_to 'Voltar', itens_path, :class => "btn btn-primary colorprimarybtn"%>
            <input type="submit" name="commit" class="btn btn-primary colorprimarybtn" value="Salvar " data-disable-with="Salvando" />
          </div>
      </div>
<% end %>